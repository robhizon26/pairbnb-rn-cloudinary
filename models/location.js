export class PlaceLocation {
  constructor(lat, lng, address, staticMapImageUrl) {
    this.lat = lat;
    this.lng = lng;
    this.address = address;
    this.staticMapImageUrl = staticMapImageUrl;
  }
}
